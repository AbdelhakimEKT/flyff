@page
@model Hellion.Web.Pages.ErrorModel
@{
    ViewData["Title"] = "Error";
}

<h2>Error</h2>

<p>An error occurred while processing your request.</p>

@if (Model.ShowRequestId)
{
    <p class="muted">Request ID: <code>@Model.RequestId</code></p>
}
